import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// Konfigurasi Vite standar untuk React + TypeScript.
// Tidak ada yang perlu diubah di sini untuk kebutuhan project ini.
export default defineConfig({
  plugins: [react()],
});
